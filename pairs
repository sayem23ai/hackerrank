int pairs(int k, vector<int> arr) {
    int n=arr.size();
    int count=0;
    unordered_map<int, int>mp;
    for(int i=0;i<n;i++){
        count+=mp[arr[i]-k];
        count+=mp[arr[i]+k];
        mp[arr[i]]+=1;
    }
    return count++;
}
