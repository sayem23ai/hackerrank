vector<int> rotateLeft(int d, vector<int> arr) {
    int n = arr.size();
    while (d > 0) {
        int temp = arr[0];
        int i = 0;
        while (i < n - 1) {
            arr[i] = arr[i + 1];
            i++;
        }
        arr[n - 1] = temp;
        d--;
    }
    return arr;
}
